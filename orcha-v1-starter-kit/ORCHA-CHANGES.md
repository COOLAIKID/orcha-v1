# Orcha changes

Append-only log of what moved in the source tree, newest at the bottom.
Written by `crew-watch.ps1`. Read this before editing a surface someone else touched.


## 2026-08-22 16:34

Edited:
- `ui\src\styles.css` (same length)


## 2026-08-22 16:35

Edited:
- `ui\src\styles.css` (+31 lines)


## 2026-08-22 16:35

Edited:
- `ui\src\styles.css` (+3 lines)


## 2026-08-22 16:45

Edited:
- `ui\src\styles.css` (+12 lines)
- `ui\src\components\Shell.tsx` (-1 lines)


## 2026-08-22 16:46

Edited:
- `ui\src\styles.css` (+2 lines)


## 2026-08-22 16:47

Edited:
- `ui\src\styles.css` (same length)


## 2026-08-22 16:48

Edited:
- `ui\src\components\Shell.tsx` (-3 lines)


## 2026-08-22 16:55

Edited:
- `ui\src\views\ChatEntry.tsx` (+3 lines)
- `ui\src\components\Shell.tsx` (+1 lines)

Added:
- `ui\src\views\CompanyOnboard.tsx`


## 2026-08-22 16:56

Edited:
- `ui\src\styles.css` (+171 lines)
- `ui\src\views\ChatEntry.tsx` (+30 lines)


## 2026-08-22 17:21

Edited:
- `ui\src\styles.css` (+25 lines)
- `ui\src\views\CompanyOnboard.tsx` (+14 lines)


## 2026-08-22 17:22

Edited:
- `ui\src\views\CompanyOnboard.tsx` (-2 lines)


## 2026-08-22 17:23

Edited:
- `ui\src\styles.css` (+45 lines)
- `ui\src\views\CompanyOnboard.tsx` (+2 lines)


## 2026-08-22 17:24

Edited:
- `ui\src\styles.css` (-1 lines)
- `ui\src\views\CompanyOnboard.tsx` (same length)


## 2026-08-22 17:25

Edited:
- `ui\src\views\CompanyOnboard.tsx` (-2 lines)


## 2026-08-22 17:25

Edited:
- `ui\src\styles.css` (-9 lines)


## 2026-08-22 17:26

Edited:
- `ui\src\styles.css` (-3 lines)


## 2026-08-22 17:29

Edited:
- `ui\src\styles.css` (-4 lines)
- `ui\src\views\CompanyOnboard.tsx` (+2 lines)
- `ui\src\data.ts` (+1 lines)


## 2026-08-22 17:30

Edited:
- `ui\src\views\CompanyOnboard.tsx` (+5 lines)


## 2026-08-22 17:31

Edited:
- `ui\src\data.ts` (same length)
- `ui\src\views\CompanyOnboard.tsx` (same length)
- `ui\src\components\Shell.tsx` (same length)


## 2026-08-22 17:32

Edited:
- `ui\src\views\CompanyOnboard.tsx` (same length)


## 2026-08-22 17:32

Edited:
- `ui\src\views\CompanyOnboard.tsx` (-3 lines)


## 2026-08-22 17:34

Edited:
- `ui\src\data.ts` (+50 lines)
- `ui\src\views\CompanyOnboard.tsx` (+1 lines)
- `ui\src\types.ts` (same length)


## 2026-08-22 17:34

Edited:
- `ui\src\views\CompanyOnboard.tsx` (+4 lines)


## 2026-08-22 17:35

Edited:
- `ui\src\views\CompanyOnboard.tsx` (+28 lines)


## 2026-08-22 17:36

Edited:
- `ui\src\data.ts` (same length)
- `ui\src\views\CompanyOnboard.tsx` (same length)


## 2026-08-22 17:37

Edited:
- `ui\src\views\CompanyOnboard.tsx` (same length)


## 2026-08-22 17:37

Edited:
- `ui\src\views\CompanyOnboard.tsx` (same length)


## 2026-08-22 17:40

Edited:
- `ui\src\views\CompanyOnboard.tsx` (+3 lines)


## 2026-08-22 17:40

Edited:
- `ui\src\views\CompanyOnboard.tsx` (+2 lines)


## 2026-08-22 17:41

Edited:
- `ui\src\views\CompanyOnboard.tsx` (same length)


## 2026-08-22 17:42

Edited:
- `ui\src\views\CompanyOnboard.tsx` (same length)


## 2026-08-22 17:43

Edited:
- `ui\src\styles.css` (+1 lines)
- `ui\src\views\CompanyOnboard.tsx` (same length)


## 2026-08-22 17:43

Edited:
- `ui\src\views\CompanyOnboard.tsx` (same length)


## 2026-08-22 17:44

Edited:
- `ui\src\views\CompanyOnboard.tsx` (same length)


## 2026-08-22 17:45

Edited:
- `ui\src\styles.css` (+13 lines)
- `ui\src\views\CompanyOnboard.tsx` (same length)


## 2026-08-22 17:45

Edited:
- `ui\src\styles.css` (+5 lines)


## 2026-08-22 17:46

Edited:
- `ui\src\styles.css` (+35 lines)
- `ui\src\views\CompanyOnboard.tsx` (same length)


## 2026-08-22 17:47

Edited:
- `ui\src\views\CompanyOnboard.tsx` (same length)


## 2026-08-22 18:11

Edited:
- `ui\src\styles.css` (+24 lines)
- `ui\index.html` (same length)


## 2026-08-22 18:21

Edited:
- `ui\src\views\ChatEntry.tsx` (+12 lines)
- `ui\src\styles.css` (+1 lines)


## 2026-08-22 18:29

Edited:
- `ui\src\styles.css` (+1 lines)


## 2026-08-22 18:32

Edited:
- `ui\src\views\ChatEntry.tsx` (+2 lines)


## 2026-08-22 18:43

Edited:
- `ui\src\views\ChatEntry.tsx` (+6 lines)


## 2026-08-22 18:55

Edited:
- `ui\src\views\ChatEntry.tsx` (+2 lines)


## 2026-08-22 18:56

Edited:
- `ui\src\styles.css` (+21 lines)
- `ui\src\views\ChatEntry.tsx` (+2 lines)


## 2026-08-22 18:56

Edited:
- `ui\src\styles.css` (+11 lines)
- `ui\src\views\ChatEntry.tsx` (same length)


## 2026-08-22 18:58

Edited:
- `ui\src\views\ChatEntry.tsx` (same length)


## 2026-08-22 20:28

Edited:
- `ui\src\styles.css` (+6 lines)
- `ui\src\views\ChatEntry.tsx` (+4 lines)


## 2026-08-22 20:28

Edited:
- `ui\src\views\ChatEntry.tsx` (+2 lines)


## 2026-08-22 20:31

Edited:
- `ui\src\views\ChatEntry.tsx` (+4 lines)
- `ui\src\styles.css` (same length)


## 2026-08-22 20:55

Edited:
- `ui\src\styles.css` (-63 lines)
- `ui\src\views\ChatEntry.tsx` (-18 lines)


## 2026-08-22 20:58

Edited:
- `ui\src\views\ChatEntry.tsx` (-4 lines)
- `ui\src\styles.css` (-1 lines)


## 2026-08-22 21:02

Edited:
- `ui\src\views\ChatEntry.tsx` (-3 lines)
- `ui\index.html` (-2 lines)


## 2026-08-22 21:02

Edited:
- `ui\src\views\ChatEntry.tsx` (-7 lines)


## 2026-08-22 21:04

Edited:
- `ui\src\views\ChatEntry.tsx` (same length)


## 2026-08-22 21:06

Edited:
- `ui\src\views\ChatEntry.tsx` (same length)


## 2026-08-22 21:11

Edited:
- `ui\src\views\CompanyOnboard.tsx` (-4 lines)
- `ui\src\styles.css` (+1 lines)


## 2026-08-22 21:12

Edited:
- `ui\src\styles.css` (same length)


## 2026-08-22 21:12

Edited:
- `ui\src\styles.css` (same length)


## 2026-08-22 21:14

Edited:
- `ui\src\styles.css` (same length)


## 2026-08-22 21:18

Edited:
- `ui\src\views\ChatEntry.tsx` (+10 lines)


## 2026-08-22 21:19

Edited:
- `ui\src\views\ChatEntry.tsx` (+15 lines)
- `ui\src\styles.css` (+8 lines)


## 2026-08-22 21:19

Edited:
- `ui\src\styles.css` (+5 lines)


## 2026-08-22 21:20

Edited:
- `ui\src\styles.css` (+3 lines)
- `ui\src\views\ChatEntry.tsx` (same length)


## 2026-08-22 21:30

Edited:
- `ui\src\views\ChatEntry.tsx` (same length)


## 2026-08-22 21:41

Edited:
- `AGENTS.md` (+1 lines)
- `START_HERE.md` (+1 lines)


## 2026-08-22 21:45

Edited:
- `AGENTS.md` (+1 lines)
- `START_HERE.md` (same length)


## 2026-08-22 21:46

Edited:
- `ui\src\views\ChatEntry.tsx` (-1 lines)

Added:
- `ui\src\blueField.ts`


## 2026-08-22 21:47

Edited:
- `ui\src\views\ChatEntry.tsx` (-41 lines)
- `ui\src\data.ts` (-5 lines)


## 2026-08-22 21:47

Edited:
- `ui\src\views\CompanyOnboard.tsx` (+1 lines)


## 2026-08-22 21:48

Edited:
- `ui\src\views\CompanyOnboard.tsx` (+18 lines)


## 2026-08-22 21:49

Edited:
- `ui\src\views\CompanyOnboard.tsx` (+37 lines)


## 2026-08-22 21:49

Edited:
- `ui\src\views\CompanyOnboard.tsx` (-5 lines)


## 2026-08-22 21:50

Edited:
- `ui\src\styles.css` (+3 lines)
- `ui\src\views\CompanyOnboard.tsx` (-1 lines)


## 2026-08-22 21:50

Edited:
- `ui\src\styles.css` (+1 lines)


## 2026-08-22 21:51

Edited:
- `ui\src\styles.css` (same length)


## 2026-08-22 21:52

Edited:
- `ui\src\views\CompanyOnboard.tsx` (same length)


## 2026-08-22 21:52

Edited:
- `ui\src\views\CompanyOnboard.tsx` (-3 lines)


## 2026-08-22 21:53

Edited:
- `ui\src\views\CompanyOnboard.tsx` (same length)


## 2026-08-22 21:53

Edited:
- `ui\src\views\CompanyOnboard.tsx` (same length)


## 2026-08-22 21:58

Edited:
- `ui\src\styles.css` (+12 lines)
- `ui\src\views\CompanyOnboard.tsx` (+2 lines)


## 2026-08-22 21:59

Edited:
- `ui\src\views\CompanyOnboard.tsx` (+69 lines)


## 2026-08-22 22:00

Edited:
- `ui\src\styles.css` (same length)


## 2026-08-22 22:16

Edited:
- `ui\src\views\CompanyOnboard.tsx` (+102 lines)


## 2026-08-22 22:17

Edited:
- `ui\src\views\CompanyOnboard.tsx` (-67 lines)
- `ui\src\styles.css` (-2 lines)


## 2026-08-22 22:18

Edited:
- `ui\src\views\CompanyOnboard.tsx` (-17 lines)


## 2026-08-22 22:28

Edited:
- `ui\src\styles.css` (+4 lines)
- `ui\src\views\CompanyOnboard.tsx` (+2 lines)


## 2026-08-22 22:28

Edited:
- `ui\src\views\CompanyOnboard.tsx` (+2 lines)
- `ui\src\styles.css` (same length)


## 2026-08-22 22:36

Edited:
- `ui\src\types.ts` (+11 lines)
- `ui\src\views\CompanyOnboard.tsx` (same length)


## 2026-08-22 22:37

Edited:
- `ui\src\views\CompanyOnboard.tsx` (-5 lines)
- `ui\src\styles.css` (same length)


## 2026-08-22 22:37

Edited:
- `ui\src\styles.css` (-2 lines)
- `ui\src\views\CompanyOnboard.tsx` (-1 lines)


## 2026-08-22 22:38

Edited:
- `ui\src\views\CompanyOnboard.tsx` (same length)


## 2026-08-22 22:41

Edited:
- `ui\src\views\CompanyOnboard.tsx` (+7 lines)
- `ui\src\views\ChatEntry.tsx` (+1 lines)


## 2026-08-22 22:42

Edited:
- `ui\src\styles.css` (+9 lines)
- `ui\src\views\CompanyOnboard.tsx` (+6 lines)


## 2026-08-22 22:43

Edited:
- `ui\src\styles.css` (+13 lines)


## 2026-08-22 23:01

Edited:
- `ui\src\views\CompanyOnboard.tsx` (+2 lines)
- `ui\src\styles.css` (same length)


## 2026-08-22 23:01

Edited:
- `ui\src\styles.css` (+6 lines)


## 2026-08-22 23:03

Edited:
- `ui\src\views\CompanyOnboard.tsx` (+15 lines)
- `ui\src\styles.css` (+5 lines)


## 2026-08-22 23:07

Edited:
- `ui\src\styles.css` (-3 lines)
- `ui\src\views\CompanyOnboard.tsx` (-2 lines)


## 2026-08-22 23:07

Edited:
- `ui\src\styles.css` (+15 lines)


## 2026-08-22 23:10

Edited:
- `ui\src\views\CompanyOnboard.tsx` (+4 lines)


## 2026-08-22 23:15

Edited:
- `ui\src\views\CompanyOnboard.tsx` (-10 lines)


## 2026-08-22 23:17

Edited:
- `ui\src\views\CompanyOnboard.tsx` (-6 lines)
- `ui\src\components\Shell.tsx` (+2 lines)
- `ui\src\views\ChatEntry.tsx` (-1 lines)


## 2026-08-22 23:17

Edited:
- `ui\src\views\CompanyOnboard.tsx` (-1 lines)
- `ui\src\views\ChatEntry.tsx` (same length)


## 2026-08-22 23:18

Edited:
- `ui\src\styles.css` (-12 lines)
- `ui\src\views\ChatEntry.tsx` (same length)


## 2026-08-22 23:18

Edited:
- `ui\src\styles.css` (-1 lines)


## 2026-08-26 14:00

Edited:
- `ui\src\runtimeClient.ts` (bounded runtime readiness polling for `/workspace-check`)
- `ui\src\runtimeClient.test.ts` (cold WSL starting-state regression)
- `src\orcha\sandbox\local_wsl.py` (truthful cached/background health refresh)
- `src\orcha\runtime\local_runtime.py` (bounded ready wait and timeout boundary)
- `docs\operations\LOCAL_RUNTIME.md` (documented readiness polling and cache)
- `ORCHA-FIELD-STUDY.md` (crew note)

Validated:
- Backend: 50 passed
- Frontend: 33 passed; TypeScript check and production build passed
- Live `/workspace-check`: real event sequence and physical `test.txt` verified

## 2026-08-26 14:30

Edited:
- `ui\src\runtimeClient.ts` (abort-safe readiness and event polling)
- `ui\src\runtimeClient.test.ts` (readiness cancellation regression)
- `ui\src\views\ChatEntry.tsx` (Stop/reset/navigation cancellation boundary)
- `ORCHA-FIELD-STUDY.md` (crew note)

Validated:
- Frontend: 34 tests passed; TypeScript check and production build passed
- A cold-start `/workspace-check` can be stopped before company creation or
  task dispatch; an already accepted task still uses the server stop path

## 2026-08-26 15:00

Edited:
- `src\orcha\runtime\local_runtime.py` (replaceable runtime service protocol)
- `src\orcha\api\app.py` (runtime dependency injection)
- `src\orcha\worker\app.py` (bounded file reads)
- `tests\test_worker_and_runtime.py` (injected runtime regression)
- `docs\api\API_CONTRACTS.md` (provider injection seam)
- `ORCHA-FIELD-STUDY.md` (crew note)

Validated:
- Frontend: 34 tests passed; TypeScript check and production build passed
- Backend runtime and worker behavior remain covered by the existing suite;
  local uv test launcher requires a follow-up environment fix for a reliable
  in-process exit summary

## 2026-08-26 15:30

Edited:
- `src\orcha\feedback\service.py` (per-error diagnostics bounds and serialized local sink writes)
- `src\orcha\worker\app.py` (bounded file reads)
- `tests\test_worker_and_runtime.py` (feedback and large-read regressions)
- `ORCHA-FIELD-STUDY.md` (crew note)

Validated:
- Focused worker, runtime injection, and feedback tests: 3 passed
- Python compileall, frontend TypeScript check, and live workspace-check passed

## 2026-08-26 16:00

Validated:
- Reloaded API on `127.0.0.1:8080`; live feedback submission returned `received`
- Runtime health transitioned from truthful `starting` to `ready` with the
  dedicated worker healthy
- Shared Vite preview returned `200` at `http://127.0.0.1:4173/`

## 2026-08-26 16:30

Edited:
- `ui\\src\\runtimeClient.ts` (bounded individual health requests and pause
  Demo before a real workspace check begins)
- `ui\\src\\runtimeClient.test.ts` (hung localhost bridge timeout regression)
- `ORCHA-FIELD-STUDY.md` (crew note)

Validated:
- Frontend: 35 tests passed; direct TypeScript check and production build passed
- A hung `/v1/runtime/health` request now resolves to a truthful offline state
  within its request budget instead of holding readiness indefinitely

## 2026-08-26 17:00

Edited:
- `src\\orcha\\api\\app.py` (feedback no longer probes the worker without
  diagnostics consent)
- `tests\\test_worker_and_runtime.py` (offline-independent feedback regression)
- `docs\\api\\API_CONTRACTS.md` (feedback availability/privacy boundary)
- `ORCHA-FIELD-STUDY.md` (crew note)

Validated:
- Ordinary feedback submits without reading runtime health
- Opted-in diagnostics still read the runtime version once

## 2026-08-26 17:30

Edited:
- `src\\orcha\\sandbox\\local_wsl.py` (strict localhost worker URL boundary)
- `tests\\test_worker_and_runtime.py` (remote URL rejection regression)
- `docs\\security\\SECURITY_BOUNDARIES.md` (local/cloud provider separation)
- `ORCHA-FIELD-STUDY.md` (crew note)

Validated:
- Local WSL provider accepts only `http://127.0.0.1:8765`
- Remote worker routing remains reserved for `CloudSandboxManager`

## 2026-08-26 18:00

Edited:
- `src\\orcha\\sandbox\\local_wsl.py` (keep worker auth tokens out of
  visible WSL process arguments)
- `tests\\test_worker_and_runtime.py` (bridge token and tracked-process stop
  regressions)
- `docs\\security\\SECURITY_BOUNDARIES.md` (bridge secret handling)
- `ORCHA-FIELD-STUDY.md` (crew note)

Validated:
- The Windows bridge carries the optional token through a child environment
- `stop_all` terminates a tracked long-running worker process and clears it

## 2026-08-26 18:30

Edited:
- `src\\orcha\\sandbox\\local_wsl.py` (minimal WSL bridge environment)
- `tests\\test_worker_and_runtime.py` (host PATH/token exclusion regression)
- `docs\\security\\SECURITY_BOUNDARIES.md` (minimal child environment)
- `ORCHA-FIELD-STUDY.md` (crew note)

Validated:
- Bridge startup no longer inherits the host's arbitrary tool PATHs
- Optional worker auth remains environment-only and absent from command args

## 2026-08-26 19:00

Edited:
- `ui\\src\\views\\ChatEntry.tsx` (clear the composer Busy state after every
  workspace-check outcome)
- `ORCHA-FIELD-STUDY.md` (crew note)

Validated:
- `/workspace-check` no longer leaves the consumer composer stuck in Stop mode

## 2026-08-26 19:30

Validated:
- Final backend suite after the bridge hardening: 54 passed, 1 existing
  Starlette/httpx deprecation warning
- Final frontend suite: 35 passed; TypeScript and production build passed
- Reloaded API and real worker proof remained healthy; shared preview returned
  `200` at `http://127.0.0.1:4173/`
- Real workspace-check again produced the expected task event sequence and
  `test.txt` with `hello from orcha`

## 2026-08-26 20:00

Edited:
- `src\\orcha\\runtime\\local_runtime.py` (deduplicate in-flight workspace checks per company)
- `scripts\\setup-orcha-worker.ps1` (disable Windows PATH interop in the dedicated distro)
- `docs\\operations\\LOCAL_RUNTIME.md` (lifecycle and duplicate-check notes)
- `tests\\test_worker_and_runtime.py` (active-job reuse regression)

Validated:
- Concurrent retries for one company reuse the existing task and cannot create
  a second workspace writer or duplicate `task.created` event
- New worker setup keeps host executable paths out of WSL PATH translation

## 2026-08-26 20:30

Edited:
- `scripts\\install-orcha-startup.ps1` (idempotent current-user logon task)
- `README.md` (local continuity instructions)
- `docs\\operations\\LOCAL_RUNTIME.md` (startup-task boundary)
- `docs\\operations\\DEPLOYMENT_PLAN.md` (Windows pilot lifecycle)

Validated:
- Startup registration is optional, scoped to the current user, and supports
  explicit uninstall without touching other scheduled tasks
- The documented task starts the existing worker/API wrapper only; it does not
  claim cloud execution or operation while the PC is powered off

## 2026-08-26 21:00

Edited:
- `src\\orcha\\worker\\app.py` (global `runtime` stop sentinel)
- `src\\orcha\\runtime\\local_runtime.py` (best-effort worker cleanup at API shutdown)
- `tests\\test_worker_and_runtime.py` (multi-company process cleanup regression)
- `docs\\operations\\LOCAL_RUNTIME.md` (shutdown contract)

Validated:
- A global stop terminates tracked children belonging to multiple companies
- API shutdown attempts cleanup without turning worker unavailability into a
  shutdown failure

## 2026-08-26 21:30

Edited:
- `src\\orcha\\runtime\\local_runtime.py` (mark workspace checks as runtime tasks)
- `src\\orcha\\persistence\\sqlite.py` (fail-closed restart handling for runtime jobs)
- `src\\orcha\\runtime\\scheduler.py` (publish cancellation instead of requeueing orphaned runtime work)
- `tests\\test_worker_and_runtime.py` (restart execution-class regression)
- `docs\\operations\\LOCAL_RUNTIME.md` (restart behavior)

Validated:
- Interrupted specialist tasks still requeue to the scheduler
- Interrupted workspace checks become cancelled and require an explicit retry
- A runtime task can never be accidentally dispatched as specialist work after restart

## 2026-08-26 22:00

Edited:
- `src\\orcha\\feedback\\service.py` (defense-in-depth diagnostic redaction)
- `ui\\src\\diagnostics.ts` (browser-side credential, URL, and path redaction)
- `tests\\test_worker_and_runtime.py` (raw secret/path redaction regression)
- `docs\\api\\API_CONTRACTS.md` (feedback privacy contract)
- `docs\\operations\\LOCAL_RUNTIME.md` (feedback persistence boundary)

Validated:
- Opted-in diagnostics redact labeled and raw credential forms, environment assignments, URLs, and common absolute paths before persistence

## 2026-08-26 22:30

Edited:
- `src\\orcha\\sandbox\\local_wsl.py` (send bridge request bodies over stdin)
- `tests\\test_worker_and_runtime.py` (protect workspace payloads from process arguments)
- `docs\\operations\\LOCAL_RUNTIME.md` and `docs\\security\\SECURITY_BOUNDARIES.md` (document bridge transport boundary)

Validated:
- Windows-to-WSL file content no longer appears in the visible `wsl.exe` argument list
- The bridge remains compatible with health and write-file requests

## 2026-08-26 23:00

Edited:
- `src\\orcha\\feedback\\service.py` (fail closed to same-app pathnames)
- `tests\\test_worker_and_runtime.py` (external-origin diagnostics regression)
- `docs\\api\\API_CONTRACTS.md` (pathname-only feedback contract)

Validated:
- Opted-in diagnostics cannot persist a crafted external route or protocol-relative origin

## 2026-08-26 23:30

Edited:
- `ui\\vite.config.ts` and `ui\\src\\chatPlugin.ts` (explicit module extensions for Vite's upcoming native config loader)

Validated:
- Development chat middleware imports remain unchanged while the Vite config build warning is removed

## 2026-08-27 00:00

Edited:
- `src\\orcha\\sandbox\\local_wsl.py` (recheck cached Windows loopback readiness)
- `tests\\test_worker_and_runtime.py` (worker-disappearance health regression)
- `docs\\operations\\LOCAL_RUNTIME.md` (truthful cached-ready behavior)

Validated:
- A worker that leaves after a ready probe is reported as starting/offline instead of remaining falsely ready

## 2026-08-27 00:15

Edited:
- `src\\orcha\\sandbox\\local_wsl.py` (preserve explicit offline health responses)
- `tests\\test_worker_and_runtime.py` (complete ready-to-offline transition coverage)

Validated:
- Local Workspace health preserves the full ready/starting/offline contract across cached and bridge probes

## 2026-08-26 14:20

Edited:
- `scripts\start-orcha-tunnel.ps1` (repeatable, origin-verified temporary Cloudflare Quick Tunnel)
- `scripts\stop-orcha-tunnel.ps1` (scoped tunnel shutdown)
- `README.md` and `docs\operations\LOCAL_RUNTIME.md` (phone-share workflow and privacy boundary)

Validated:
- Local Vite origin, API, and worker were reachable
- A fresh Quick Tunnel was created and publicly verified at the printed URL
- The tunnel exposes only the Vite surface; API and worker ports remain private

## 2026-08-26 14:35

Edited:
- `scripts\start-orcha-tunnel.ps1` and `scripts\stop-orcha-tunnel.ps1` (PID-reuse-safe tunnel ownership)
- `src\orcha\api\app.py` (mobile-friendly SSE retry hint and no-transform cache boundary)
- `docs\operations\LOCAL_RUNTIME.md` (reconnect and process-safety notes)

Validated:
- A recorded tunnel PID is stopped only when its executable and launch time still match
- Runtime event clients receive a bounded reconnect hint through the temporary tunnel

## 2026-08-26 15:10

Edited:
- `src\orcha\api\app.py` (disconnect-aware async runtime SSE stream)
- `ui\src\runtimeClient.ts` and `ui\src\runtimeClient.test.ts` (cursor-preserving reconnect with bounded backoff)
- `docs\operations\LOCAL_RUNTIME.md` (mobile reconnect behavior)

Validated:
- Disconnected event streams can exit without retaining a sleeping server worker
- Browser reconnects resume from the latest durable event sequence

## 2026-08-26 15:45

Edited:
- `src\orcha\sandbox\cloud.py` (preserve explicit remote worker health states)
- `infra\docker-compose.yml` (pass the shared worker auth token to the API)
- `tests\test_worker_and_runtime.py` (cloud health regression)
- `docs\operations\DEPLOYMENT_PLAN.md` (cloud pilot auth/health contract)

Validated:
- The cloud replacement preserves `offline`, runtime version, workspace, and detail fields
- Docker Compose can authenticate API-to-worker requests with the same secret

## 2026-08-26 15:52

Edited:
- `tests\test_worker_and_runtime.py` (cloud control-plane token forwarding regression)

Validated:
- `CloudSandboxManager` forwards the private worker token on hosted health requests

## 2026-08-26 15:52

Edited:
- `src\orcha\models\gateway.py` (ordered server-side temporary-key pools)
- `.env.example`, `orcha.local.env.example`, `README.md`, and `docs\operations\LOCAL_RUNTIME.md` (key-pool setup)
- `tests\test_worker_and_runtime.py` (same-provider key fallback regression)

Validated:
- A rejected temporary key falls through to the next deduplicated key without changing provider identity or exposing key material

## 2026-08-26 16:05

Edited:
- `src\orcha\models\gateway.py` (process-local cooldown for failed temporary provider keys)
- `.env.example`, `orcha.local.env.example`, and `docs\operations\LOCAL_RUNTIME.md` (cooldown setting and recovery behavior)
- `tests\test_worker_and_runtime.py` (next-call key rotation regression)

Validated:
- A failed pool key is skipped on the next agent call while a healthy key remains available
- Full backend suite: 66 passed, 1 existing Starlette/httpx deprecation warning
- Fallback telemetry still names the originally preferred provider when its key is cooling

## 2026-08-26 16:30

Edited:
- `scripts\watch-orcha-local.ps1` (scoped local API/WSL supervisor with single-pass mode)
- `scripts\install-orcha-startup.ps1` (logon task now runs the supervisor)
- `README.md`, `docs\operations\LOCAL_RUNTIME.md`, and `docs\operations\DEPLOYMENT_PLAN.md` (PC continuity limits and recovery behavior)

Validated:
- Supervisor syntax parses and its single safe health pass reports the existing local runtime without restarting healthy services

## 2026-08-26 16:45

Edited:
- `ui\src\views\ChatEntry.tsx` (mobile-safe headline field rendering, deterministic left-to-right sweep, resize-only layout reads, hidden-tab animation pause)
- `ui\src\styles.css` (CSS text clipping replaces per-frame SVG pattern invalidation)
- `ui\src\blueField.ts` (soft highlight lane so the sweep remains legible across words)

Validated:
- Active consumer preview renders the same font and moving `?` with zero headline SVG nodes
- Field background position changes during the live sweep and the verb continues cycling
- Frontend TypeScript and Vite production build pass
- Impeccable detector run completed; remaining warnings are existing font/grid/onboarding patterns outside this motion pass

## 2026-08-26 17:05

Edited:
- `ui\src\tunnelGate.ts` (opt-in constant-time pilot token check and hashed HttpOnly cookie contract)
- `ui\src\chatPlugin.ts` (protect `/v1` and `/api/*` on shared Vite/Cloudflare previews while keeping local development open when unset)
- `ui\src\node-shims.d.ts` (minimal crypto declarations for the dev-server gate)
- `ui\.env.example`, `README.md`, `docs\operations\LOCAL_RUNTIME.md`, `docs\operations\DEPLOYMENT_PLAN.md`, and `docs\security\SECURITY_BOUNDARIES.md` (friends-only temporary share guidance and limits)

Validated:
- Gate is opt-in and does not change the current local preview without `ORCHA_TUNNEL_TOKEN`
- Tokens are server-side only; the access redirect removes the query token and stores only a SHA-256 digest in an HttpOnly, SameSite cookie
- Protected paths return an explicit access-required response before the existing Vite/API handlers run

## 2026-08-26 — Durable task controls

- Added `GET /v1/companies/{id}/tasks` plus owner pause/retry endpoints with
  strict company ownership checks and fail-closed handling for active tasks.
- Added durable `task.paused` and `task.retry_requested` event mappings in the
  real Agent Grid and work log. Retries reopen a failed/stopped/blocked parent
  run only when the company is running; runtime control jobs keep their
  dedicated action boundary.
- Hardened scheduler dispatch by re-reading queued task state immediately
  before submission, preventing stale queue snapshots from undoing a pause.
- Added backend and UI tests covering the new event and control behavior.

## 2026-08-26 — Task-control reconciliation

- Retry now resets the bounded task-attempt window and records the prior
  attempt count; runtime control jobs stay behind their dedicated actions.
- Paused tasks remain pending in terminal/open-work accounting and are canceled
  by Stop/teardown, preventing false run completion or an always-on cycle from
  skipping owner-held work.
- Agent Grid and work log now project both `task.resumed` and
  `task.retry_requested` as explicit queued-again work.

## 2026-08-26 — Restart projection reconciliation

- `src\orcha\runtime\scheduler.py` now resets an interrupted specialist's
  durable Agent row to `waiting` before requeueing it, then emits a
  `recovered: true` status event and `task.recovered`.
- `ui\src\runtimeEvents.ts` now keeps explicit `waiting` and `created` agent
  states waiting instead of projecting them as working.
- Added backend/UI regression coverage and documented the fail-closed runtime
  control-job behavior in `docs\operations\LOCAL_RUNTIME.md` and
  `docs\api\EVENT_SCHEMAS.md`.

- Work-log projection now applies the same explicit waiting-state mapping, so
  restart recovery does not leave a specialist counted as active.

## 2026-08-26 — Idempotent artifact retries

- `Artifact` records now retain an optional SHA-256 content hash, and the
  bounded agent runner reuses an identical task/path/hash instead of issuing a
  duplicate workspace write after a retry.
- Changed retry output remains writable so QA recovery can still repair a
  generated file. Added a regression test and documented the behavior in the
  data model and local-runtime guide.

- The worker now treats an identical existing `write_file` as a bounded no-op
  and refuses oversized existing files before reading them for comparison.
  This closes the crash-between-write-and-artifact-record retry window without
  broadening the worker command surface.

## 2026-08-26 — Restart run reconciliation

- Added durable run listing and scheduler startup reconciliation. If terminal
  tasks committed but run finalization did not, the scheduler emits the missing
  `company.run_completed` transition once and schedules the next always-on
  cycle from the repaired state.
- Added a regression test and documented the ordering guarantee in the local
  runtime and event-schema guides.
- Corrected the implemented event inventory to include the existing
  `agent.created` and `agent.status_changed` lifecycle events.

## 2026-08-26 — Durable task leases

- `Task` now persists a short lease id/timestamp; SQLite claims queued work
  atomically before scheduler dispatch and clears leases on restart recovery,
  terminal completion, and retry requeue.
- Added an atomic-claim regression test and documented the coordination
  contract in the data model and local-runtime guide.

## 2026-08-26 — Chat surface focus boundaries

- The animated chat sidebar now becomes inert while off-canvas, moves focus to
  its first available control when opened, cycles focus while open, and returns
  focus to the brand trigger when closed.
- Feedback now traps keyboard focus inside its dialog and restores the prior
  focus target after dismissal; the sent state focuses its Done action.
- No visual layout, chat copy, runtime behavior, or sidebar information
  architecture changed.

## 2026-08-26 — Touch-target hardening

- On narrow or coarse-pointer surfaces, the chat brand trigger, sidebar switch,
  menu controls, composer actions, and feedback type controls now meet the
  44px touch-target floor.
- Composer padding expands with the action hit areas so text and controls keep
  clear separation; the desktop mouse layout is unchanged.

## 2026-08-26 — Agent Grid exit affordance

- Agent Grid now remains mounted for a short, reduced-motion-aware exit phase
  instead of disappearing immediately when closed.
- Added the missing 44px top-right Close Agent Grid control and routed Escape,
  the close event, and the control through the same exit path.

## 2026-08-26 — Worker path normalization under concurrent starts

- Normalized resolved Windows extended-prefix paths before containment checks,
  so simultaneous company workspace creation cannot reject a valid child path.
- The global worker stop-all path now reliably tracks and stops children from
  multiple companies without weakening symlink-aware containment.

## 2026-08-26 — Agent tool-output redaction

- Bounded stdout/stderr and follow-up tool context now redact obvious provider
  keys, bearer tokens, JWTs, and labeled environment assignments before they
  cross the agent/event boundary.
- Added regression coverage and documented this as defense in depth; the
  worker still must not be given secrets in the first place.

## 2026-08-26 — WSL source sync reliability

- Replaced the PowerShell native tar stream used by worker setup/restart with
  a direct copy through the dedicated `\\wsl$\\orcha-worker` filesystem.
- This prevents PowerShell from corrupting binary archive output and leaving
  the systemd worker in a restart loop with truncated Python source.

## 2026-08-26 — Quiet WSL wrapper environment

- Setup and restart wrappers now invoke an absolute `wsl.exe` path with an
  empty host `PATH`, so customized agent/tool paths are not translated into
  WSL during startup.

## 2026-08-26 — PowerShell wrapper defaults

- Local start, API start, supervisor, and startup-registration scripts now
  resolve their default environment file after script initialization instead
  of relying on `$PSScriptRoot` during parameter-default evaluation.
- This keeps the documented no-argument lifecycle commands compatible with
  Windows PowerShell 5.1.

## 2026-08-26 — Canonical specialist departments

- Added typed `TeamId`, bounded `HireState`, and derived `Team` snapshots to
  the backend Agent projection. Older Agent payloads infer team from role.
- Runtime task events now expose team/hiring metadata. The Agent Grid keeps
  the legacy Operations alias but maps Design, Data, and Business to distinct
  canonical departments, and exposes the hiring state in specialist detail.

## 2026-08-26 — Runtime liveness and mobile request bounds

- `src\orcha\runtime\scheduler.py` now exposes a bounded scheduler liveness
  projection, keeps transient loop/store errors from silently killing the
  dispatcher, and can recreate its executor after a stopped or unexpectedly
  exited loop.
- `/v1/runtime/health` and internal diagnostics now distinguish worker health
  from scheduler health; a ready worker is not presented as proof that the
  control plane is dispatching.
- `ui\src\runtimeClient.ts` now applies a shared 12-second timeout to runtime
  requests, including event polling and diagnostics, so a phone losing the
  temporary tunnel cannot leave controls pending indefinitely.
- Added backend and frontend regression coverage and documented the boundary
  in `docs\api\API_CONTRACTS.md` and `docs\operations\LOCAL_RUNTIME.md`.

## 2026-08-26 — Chat headline handoff polish

- The rotating consumer headline now clips its 3D face stage to one word and
  switches face ownership at the midpoint, preventing the incoming word from
  ghosting above the active word on slower mobile GPUs.
- The fix preserves the existing dark chat surface, moving glow, and question-
  mark alignment; no sidebar, runtime status chip, or demo workspace copy was
  reintroduced.

## 2026-08-26 — WSL bridge payload boundary

- Removed the serialized workspace request body from the Windows `wsl.exe`
  argument list; bridge requests now carry method/path as arguments and the
  JSON body only over stdin, matching the documented secret and source-data
  boundary.
- Strengthened the regression test to inspect the complete command line for
  workspace content and file names.

## 2026-08-26 — Held goal carried into company plan review

- The consumer chat now preserves a goal held during sign-in into the company
  onboarding plan, so the first company preview reflects the owner’s original
  sentence instead of asking them to retype it.
- Direct “new company” onboarding remains blank, and the eventual held
  message still sends through the existing chat/runtime path.

## 2026-08-26 — Specialist task start evidence

- Specialist execution now emits the documented `task.started` event alongside
  the existing agent lifecycle event, so task-level observers can distinguish a
  real dispatch boundary from a planned task.
- The event uses the existing bounded company/task/agent metadata and does not
  expose prompts, model traces, or secrets.

## 2026-08-26 — Runtime contract and planner safety hardening

- Event stores now normalize safe `companyId` and concise `summary` fields,
  with `taskId` inferred from the aggregate only for `task.*` events.
- Typed tool results with `ok: false` now stop specialist execution before a
  follow-up model response or task completion can claim success.
- Model-generated plans now reject duplicate task keys, duplicate dependencies,
  and cyclic dependency graphs; invalid plans fall back visibly.

## 2026-08-26 — Denied tool recovery state

- A specialist tool-policy denial now persists the task and agent as `blocked`
  before returning control to the scheduler, so resume and always-on accounting
  do not leave denied work stranded as `running`.

## 2026-08-26 — Empty-run restart recovery

- Durable scheduler reconciliation now fails closed when a process stops after
  saving a run but before saving any planned task rows. The run emits the
  existing terminal failure event and always-on companies receive a bounded
  next-cycle schedule instead of remaining silently idle.

## 2026-08-26 — Retry-safe company runs

- Company run creation now serializes runtime ownership changes and reuses an
  active run for the same goal. This prevents a phone or temporary tunnel
  retry from spawning duplicate plans; a different active goal returns `409`.

## 2026-08-26 — Provider cancellation boundary

- Environment model requests now carry a cancellation generation. Stop All can
  close an in-flight provider request without allowing it to fall through to a
  second temporary key or provider during unwind.

## 2026-08-26 — Honest offline chat state

- The consumer chat no longer wraps an unreachable model request in a canned
  answer or echoes the owner’s prompt as if it were generated work.
- The unavailable state now says plainly that no answer was generated and
  points to server-side provider configuration. The real chat path and
  existing workspace-check behavior remain unchanged.

## 2026-08-26 — Runtime event adapter validation

- Revalidated Cursor’s active runtime-event adapter after its file-artifact
  mapping changes. Verified file line statistics and artifact pulse labels stay
  evidence-backed, and the adapter now passes the complete frontend runtime
  test set.

## 2026-08-26 — Legacy offline history presentation

- Known offline placeholder messages from older local browser history now
  render as the same explicit Cloud AI unavailable state as new failures.
- The migration is presentation-only: stored chat records remain untouched,
  and operational states are no longer offered as AI replies to report.

## 2026-08-26 — Worker cleanup bookkeeping

- Global and company-scoped Stop All now removes stopped preview processes and
  stale child entries from the worker registry immediately.
- Repeated stop calls report only currently running children, with a regression
  covering confined preview teardown.

## 2026-08-26 — Stop wins planning races

- If the owner stops a company while its provider-backed plan is still being
  prepared, the API now persists the planned tasks as cancelled and closes the
  run without resurrecting the company or restarting the scheduler.
- This keeps Stop All authoritative across the provider wait boundary.

## 2026-08-26 — Event documentation reconciled

- Updated the event contract and observability notes to match the current local
  runtime: `task.completed` is terminal success, and distributed trace fields
  are not claimed until a hosted control plane exists.

## 2026-08-26 — Real WSL runtime proof

- Verified the installed `orcha-worker` distro is healthy on
  `127.0.0.1:8765` and runs under the unprivileged `orcha` user.
- Exercised the live FastAPI `/workspace-check` path through the Windows-to-WSL
  bridge: it created the exact `test.txt` proof, emitted durable events through
  `task.completed`, and the physical file contained exactly `hello from orcha`.
- Removed only the temporary verification company/workspace after the proof.

## 2026-08-27 — Stop All closes idle runs

- The scheduler now finalizes a company run immediately when Stop All cancels
  queued, blocked, or paused tasks and no worker remains in flight.
- In-flight tasks still own their terminal transition so their stopped-agent
  boundary is preserved; idle runs no longer remain durably `running` until a
  later restart reconciliation pass.
- Added a regression test for the queued-run stop path.

## 2026-08-27 — Stable WSL readiness without localhost forwarding

- `LocalWslSandboxManager` now remembers when the Windows localhost fast path
  is unavailable and keeps a fresh, successful WSL-bridge health result
  visible as `ready` until its configured TTL expires.
- Hosts without WSL localhost forwarding no longer show a healthy worker as
  permanently `starting`; bridge failures still transition to `offline`, and
  mutating requests remain fail-closed.
- Added coverage for repeated health reads after a bridge-confirmed ready
  result and documented the cache behavior.

## 2026-08-27 — Secret-safe provider setup helper

- Added `scripts/configure-orcha-provider.ps1` for configuring a server-side
  provider without accepting keys as command-line arguments, printing them, or
  placing them in the browser environment.
- The helper supports one provider and a bounded pool of temporary keys,
  preserves the existing local environment file, attempts to tighten its
  Windows ACL, and makes no upstream request. README and local-runtime docs now
  describe the setup and the difference between `configured` and verified.

## 2026-08-27 — Direct API loads private local configuration

- Added a dependency-free API config loader so direct `uvicorn` launches read
  the same optional `orcha.local.env` file as the Windows wrappers.
- Explicit process/container environment variables remain authoritative, local
  loading can be disabled with `ORCHA_DISABLE_LOCAL_ENV=true`, and malformed
  entries fail fast instead of silently producing an unconfigured runtime.
- Added focused loader tests for precedence, opt-out, and malformed entries.

## 2026-08-27 — Repeatable local pilot launcher

- Added `scripts/start-orcha-pilot.ps1` to start or reuse the worker, API, Vite
  cockpit, and verified temporary tunnel in the documented 5175/8080 topology.
- The launcher checks health before reuse and refuses to terminate unknown
  processes occupying required ports; `-NoTunnel` supports local-only runs.
- It discovers standard Windows Node locations and accepts an explicit
  `-NpmPath` when Node.js is installed outside `PATH`.
- UI reuse trusts a successful local HTTP probe even when the shell cannot
  enumerate the listening PID, while still refusing unhealthy occupied ports.
- Documented the shortcut while retaining the individually scoped stop scripts.

## 2026-08-27 — Stale Quick Tunnel recovery

- `start-orcha-tunnel.ps1` now recycles a dead recorded Quick Tunnel after
  validating the executable and recorded process start time, then verifies a
  fresh public URL. Unrecorded cloudflared processes remain untouched.

## 2026-08-27 — Company-scoped provider cancellation

- Company runtime Stop All now cancels only the stopped company's in-flight
  provider request instead of globally interrupting every local company.
- Scoped gateway requests use tracked short-lived HTTP clients and preserve the
  existing global cancellation path for process shutdown and legacy adapters.
- Added regressions proving a stopped company cannot fall through to a provider
  fallback while a different company's request completes normally.

## 2026-08-27 — Hardened compose pilot boundaries

- Removed `.env.example` as a live Compose `env_file`; provider values are now
  passed explicitly to the API service from a private, ignored `.env`.
- Container launches force `ORCHA_DISABLE_LOCAL_ENV=true`, keep provider keys
  out of the worker, and bind the API to `127.0.0.1:8080` instead of exposing it
  on every host interface.
- Updated the deployment and README instructions and ignored local `.env`
  files so the documented container boundary matches the actual topology.

## 2026-08-27 — Clean provider shutdown

- Added an explicit model-gateway close path for API shutdown so shared and
  in-flight scoped HTTP clients are released without allocating a replacement
  client after the control plane has stopped.
- Kept the legacy cancellation fallback for injected gateways that only expose
  the older `cancel()` contract and added a regression for idempotent cleanup.
- Closed the cancellation/close lock race so a concurrent global cancel cannot
  allocate a replacement client after shutdown has begun.

## 2026-08-27 — Cloud sandbox client lifecycle

- Added idempotent `CloudSandboxManager.close()` cleanup for an injected
  persistent worker client and fail-closed requests after shutdown.
- Added coverage proving the future cloud replacement follows the same resource
  lifecycle as the local runtime boundary.

## 2026-08-27 — Local sandbox client lifecycle

- Added idempotent cleanup for an injected `LocalWslSandboxManager` client and
  fail-closed health/request behavior after API shutdown.
- The default Windows WSL bridge remains short-lived; this closes only a
  caller-owned persistent client supplied by tests or a future adapter.

## 2026-08-27 — Resilient compose lifecycle

- Added API and worker health checks, `init` process supervision, and bounded
  30-second graceful stop windows to the container pilot.
- Added a deployment-contract regression proving the API stays loopback-only
  and provider credentials are not passed to the worker service.

## 2026-08-27 — Sanitized release handoff

- Added `RELEASE-MANIFEST.md` and indexed it in `README.md`/`CONTENTS.md` so
  the portable starter-kit boundary is explicit.
- The release archive excludes secrets, local runtime state, dependencies,
  build caches, and source-control metadata while retaining the implementation,
  tests, scripts, and product/operation truth.
- Generated `outputs/orcha-v1-starter-kit-2026-08-27.zip` with 261 entries;
  the archive scan found no excluded secret or machine-state paths.

## 2026-08-27 — Feedback control polish

- Kept the chat header minimal while making the existing Feedback action
  discoverable on desktop with a text label; the mobile breakpoint retains the
  44px icon control and the page remains free of horizontal overflow.
- Rebuilt the sanitized release archive so it includes the shared UI change;
  `.env.example` files remain intentionally included while private `.env`
  files and machine state remain excluded.

## 2026-08-27 — Reproducible release packaging

- Added `scripts/package-orcha-release.ps1` as the single directory-aware
  archive builder and exclusion validator for future handoffs.

## 2026-08-27 — Runtime company rehydration

- Reused the existing company-validation path for pause, resume, status, and
  refresh hydration so a device-local company id survives an in-memory API
  restart without surfacing a raw `Company not found` error.
- Stopping a missing runtime now clears the stale device mapping and reports a
  concise recovery action. Added direct UI regressions for stale mapping and
  control recovery.

## 2026-08-27 — Legacy runtime error presentation

- Preserved older device-local chat history while translating the exact raw
  `Company not found` control response into a concise reconnect instruction on
  load. New runtime failures continue to use their server-provided summaries.

## 2026-08-27 — Launcher environment cleanup

- API startup now restores private environment values and `PYTHONPATH` in the
  caller after handing them to the child process, including failure paths.
- Worker and combined local startup restore temporary Windows `PATH`
  isolation in `finally` blocks so an interrupted launch cannot poison the
  interactive shell.

## 2026-08-27 — Release cache exclusion correction

- Fixed the release packager to omit Python bytecode and common Python/build
  cache directories, and added those paths to the post-compression validator.
- Updated the release manifest so the sanitized boundary matches the archive
  implementation.

## 2026-08-27 — Workspace-check restart continuity

- Rehydrated one durable, still-queued workspace-check task after an API
  restart so phone/tunnel retries cannot create parallel writers.
- Kept already-running runtime tasks fail-closed on restart because their exact
  worker continuation point is unknown; updated the API and local-runtime
  contracts and added regression coverage.

## 2026-08-27 — Worker token lifecycle hardening

- Propagated the private worker token through local start, supervisor, and stop
  wrappers without putting it in visible `wsl.exe` arguments.
- Persisted the worker copy only in a root-owned systemd EnvironmentFile inside
  the dedicated distro, and added script contract coverage for the lifecycle.

## 2026-08-27 — Shared worker auth fail-closed mode

- Added `ORCHA_REQUIRE_WORKER_AUTH` to the worker and container pilot. Local
  single-machine mode remains convenient by default; shared/container mode can
  now fail with a bounded `503` when token injection is missing rather than
  silently accepting unauthenticated execution.
- Added worker and Compose contract coverage and documented the deployment
  switch. The liveness endpoint remains available to supervision and does not
  expose secret state.

## 2026-08-27 — Physical workspace verification

- Ran the live API workspace-check against a temporary company and observed
  the real `company.created`, `sandbox.connected`, `task.started`,
  `tool.started`, `file.created`, and `task.completed` sequence.
- Confirmed `/home/orcha/workspaces/<company-id>/test.txt` inside the dedicated
  `orcha-worker` distro contains exactly `hello from orcha` (16 bytes), then
  removed only the temporary verification company and its workspace.

## 2026-08-27 — Runtime secret inventory alignment

- Added worker token and required-auth variables to the Windows local
  environment example and explained the shared/container switch alongside the
  existing token forwarding contract.

## 2026-08-27 — Business specialist support

- Enabled the existing Business department as a bounded planner and runner
  role for internal pricing, legal-constraint, competitor, and support work.
  It can inspect company evidence and write an internal brief, but receives no
  publishing, messaging, payment, or external-account capability.
- Added the Business prompt and a model-plan regression so the seventh
  department cannot remain UI-only.

## 2026-08-27 — Truthful department hiring state

- Updated backend team snapshots to ignore proposed/available catalog members
  when deriving active department status. A department now stays `empty` until
  a specialist is actually hired, matching the consumer team projection.
- Added regression coverage for the inactive/unhired case.

## 2026-08-27 — Deployment readiness probe

- Added `/health/ready` as a bounded readiness boundary that requires the
  workspace provider and persistent scheduler while leaving `/health` as the
  cheap process-liveness probe.
- Switched the Compose API health check to the readiness boundary so a
  stranded scheduler or unavailable worker can be recovered by the existing
  restart policy, and added API/contract tests and documentation.

## 2026-08-27 — Supervisor readiness recovery

- Updated the Windows local supervisor to use `/health/ready`, defer API
  recovery while the worker is unavailable, and restart only an API process
  verified by the repository PID file and command line.
- Unknown processes occupying the API port remain untouched; added script
  contract coverage and lifecycle documentation.

## 2026-08-27 — Pilot launcher readiness recovery

- Updated the one-command local pilot launcher to reuse only a runtime that
  passes `/health/ready` and delegate occupied-but-unready recovery to the
  scoped supervisor.
- Kept unknown API port owners protected and documented the recovery path.

## 2026-08-27 — Stable internal specialist inbox identities

- Added a deterministic `.local` address-like identity to each durable Agent
  record and an internal-only inbox descriptor.
- Added `GET /v1/companies/{id}/inboxes` and enriched the per-agent inbox
  response so Studio can discover specialists without implying public email.
- Kept external delivery disabled and added persistence/backward-compatibility
  coverage for older agent payloads.
## 2026-08-27 — Always-on provider reconnect recovery

- Added a durable `Task.blocked_reason_code` so provider configuration blocks
  are distinguishable from policy, capability, and owner-stop blocks.
- Always-on companies now automatically reopen provider-blocked specialist
  tasks when the server-side model gateway becomes available, with bounded
  `recovery.started`, `task.resumed`, and `company.run_resumed` evidence.
- Automatic reconnect resets only the provider-blocked task's attempt window;
  no work is claimed successful and no other block class is bypassed.
## 2026-08-27 — Hosted worker readiness auth boundary

- Cloud worker readiness now fails closed when required worker authentication
  is enabled but the API has no injected token.
- Added regression coverage proving the control plane does not even probe a
  worker in that invalid configuration, while the worker's cheap `/health`
  liveness contract remains unchanged.
## 2026-08-27 — Persistent cloud worker connection

- `CloudSandboxManager` now owns one bounded HTTP client/connection pool for
  long-lived API processes instead of opening a fresh client per worker call.
- Shutdown closes the owned or injected client idempotently; the provider
  contract and required-auth readiness boundary are unchanged.
- API shutdown now also closes the app-level sandbox when a replaceable
  runtime does not own it, and the hosted pool has explicit connection limits.
## 2026-08-27 — Phone pilot continuity

- Added `watch-orcha-pilot.ps1` and an `-IncludePhonePilot` startup option so
  the current-user supervisor can recover the Vite cockpit and managed
  Cloudflare Quick Tunnel as well as the API and WSL worker after logon.
- The wrapper delegates all process ownership and stale-tunnel decisions to the
  existing scoped pilot launcher; unknown processes remain untouched.

## 2026-08-27 — Idempotent worker recovery

- Made `start-orcha-worker.ps1` reuse an already-ready worker instead of
  restarting systemd on every supervisor false-negative probe.
- Added an explicit `-ForceRestart` escape hatch for deliberate source or
  service-environment refreshes; the normal always-on path keeps active work
  uninterrupted.

## 2026-08-27 — Compose auth parity

- Passed `ORCHA_REQUIRE_WORKER_AUTH` into the API container as well as the
  private worker, so the hosted control plane enforces the documented
  fail-closed worker-auth boundary.

## 2026-08-27 — Hardened hosted Compose profile

- Added an opt-in `infra/docker-compose.hosted.yml` overlay that requires a
  worker token, keeps the API loopback-only and the worker private, and applies
  non-root/no-new-privilege resource hardening to both services.
- Added `infra/HOSTED_DEPLOYMENT.md` with the guarded start/stop workflow and
  explicit multi-tenant production dependencies.

## 2026-08-27 — Supervisor readiness hysteresis

- Made the Windows continuity supervisor retry transient `/health/ready`
  transitions and require three consecutive failed passes before restarting an
  owned API whose cheap `/health` liveness probe is still healthy.
- This preserves scheduler/worker recovery while preventing a bounded WSL
  refresh from becoming an unnecessary API restart.

## 2026-08-27 — Hosted cockpit container

- Added a production static UI container and Nginx same-origin proxy for
  `/v1/*` and `/api/*`, including streamed chat responses.
- Added a loopback-only cockpit service to the hardened hosted Compose profile;
  the worker remains private and un-published.

## 2026-08-27 — Hosted lifecycle wrappers

- Added scoped hosted start/stop PowerShell wrappers that validate Docker,
  require the profile's worker-token interpolation, wait for cockpit and API
  readiness, and retain durable volumes on shutdown.

## 2026-08-27 — Container build context boundary

- Added a root `.dockerignore` so hosted image builds do not send ignored
  environment files, local state, dependencies, caches, or non-runtime source
  to the Docker daemon; added a contract test and deployment note.

## 2026-08-27 — Unprivileged hosted cockpit

- Moved the Nginx cockpit to internal port 8080, ran it as the unprivileged
  `nginx` user, and added explicit tmpfs paths for read-only containers.
- Built all hosted images and passed a disposable three-container smoke test
  for worker/API/cockpit health and same-origin delivery.

## 2026-08-27 — Hosted boot recovery

- Added `infra/orcha-hosted.service` for Linux hosts: it validates the
  secret-safe Compose profile at boot and starts the stack after Docker, while
  retaining named volumes on stop.
